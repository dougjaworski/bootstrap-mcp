# Bootstrap CSS MCP Server Configuration

# Server Configuration
MCP_PORT=8001                    # Port 8001 to avoid conflict with tailwind-mcp (8000)
MCP_HOST=0.0.0.0
MCP_ALLOWED_HOSTS=localhost:*,127.0.0.1:*,0.0.0.0:*

# Data Directory
DATA_DIR=/app/data

# Logging Level
LOG_LEVEL=INFO
